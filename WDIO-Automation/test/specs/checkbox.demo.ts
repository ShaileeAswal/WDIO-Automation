import CheckboxPage from '../pageobjects/checkbox.page';



describe('My CheckBox Page', () => {
    it('should be able to access the checkbox page', async () => {
        await CheckboxPage.open();

    })
})